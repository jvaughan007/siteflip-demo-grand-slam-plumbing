---
---
<style>
  .hero { position: relative; min-height: 85vh; display: flex; align-items: center; justify-content: center; text-align: center; color: #fff; overflow: hidden; }
  .hero-bg { position: absolute; inset: 0; z-index: 0; }
  .hero-bg img { width: 100%; height: 100%; object-fit: cover; }
  .hero-overlay { position: absolute; inset: 0; background: linear-gradient(to bottom, rgba(0,0,0,0.55), rgba(0,0,0,0.7)); z-index: 1; }
  .hero-content { position: relative; z-index: 2; max-width: 700px; padding: 2rem 1.25rem; }
  .hero h1 { color: #fff; font-size: clamp(2.25rem, 6vw, 3.75rem); margin-bottom: 1.25rem; text-shadow: 0 2px 12px rgba(0,0,0,0.3); }
  .hero p { font-size: clamp(1rem, 2.5vw, 1.25rem); opacity: 0.9; margin-bottom: 2rem; line-height: 1.6; }
  .hero-buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
  @media (max-width: 768px) { .hero { min-height: 70vh; } }
</style>
<section class="hero">
  <div class="hero-bg"><img src="https://images.unsplash.com/photo-1585704032915-c3400ca199e7?w=1400&h=600&fit=crop" alt="" loading="eager" /></div>
  <div class="hero-overlay"></div>
  <div class="hero-content">
    <h1>Welcome to Grand Slam Plumbing</h1>
    <p>Our goal is to provide our customers with an All Star Experience by 
offering exceptional plumbing service and advice with honest upfront 
pricing.</p>
    <div class="hero-buttons">
      <a href="/contact" class="btn btn-primary">Contact Us Today</a>
      <a href="/about" class="btn btn-outline">Learn More</a>
    </div>
  </div>
</section>
